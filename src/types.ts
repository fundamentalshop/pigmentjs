interface RGB {
    readonly r: number,
    readonly g: number,
    readonly b: number,
};

interface HSL {
    readonly h: number,
    readonly s: number,
    readonly l: number,
};

type ValidatedHex = string;
type RGBString = string;
type HSLString = string;

