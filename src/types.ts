type RGB = {
    r: number,
    g: number,
    b: number,
};

type HSL = {
    h: number,
    s: number,
    l: number,
};

type ValidatedHex = string;
type RGBString = string;
type HSLString = string;

